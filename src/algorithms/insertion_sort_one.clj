(ns algorithms.insertion-sort-one)

(defn insertion-sort [ar]
  )

(def m (read-string (read-line)))
(def ar (repeatedly n (read-string (read-line))))

(insertion-sort)


^(?:
   (?:
     \((?<area>\d{3})\)) |(?<area>\d{3})[\.-]?
   )
(?<co>\d{3})[\.-]?(?<line>\d{4})
